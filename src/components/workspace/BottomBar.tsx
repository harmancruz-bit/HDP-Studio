import React, { useState } from 'react'
import { useWorkspaceContext, workspaceValue } from '../../context/WorkspaceContext'
import '../../styles/BottomBar.css'
const tabs = ['Console', 'Git', 'Deploy', 'Logs', 'Errors', 'Terminal'] as const
export default function BottomBar() { const [active, setActive] = useState<(typeof tabs)[number]>('Console'); const { currentBlock, currentModule, currentProduct, currentScreen } = useWorkspaceContext(); return <section className="bottom-bar"><nav className="bottom-tabs">{tabs.map(tab => <button key={tab} className={active === tab ? 'active' : ''} onClick={() => setActive(tab)}><span className={tab === 'Errors' ? 'tab-alert' : ''}>{tab === 'Terminal' ? '›_' : tab === 'Git' ? '⌘' : tab === 'Deploy' ? '↗' : '◌'}</span>{tab}{tab === 'Errors' && <b>0</b>}</button>)}<span className="bottom-spacer" /><button className="collapse-panel">⌃</button></nav><div className="bottom-content">{active === 'Console' ? <><div className="console-prompt"><span>›</span><span className="muted-line">Workspace Context</span></div><div className="console-context"><span>Current Product: <b>{workspaceValue(currentProduct)}</b></span><span>Current Module: <b>{workspaceValue(currentModule)}</b></span><span>Current Screen: <b>{workspaceValue(currentScreen)}</b></span><span>Current Block: <b>{workspaceValue(currentBlock)}</b></span></div></> : <div className="console-prompt"><span>›</span><span className="muted-line">{active} activity will appear here.</span></div>}<div className="console-prompt"><span>›</span><i /></div></div><div className="bottom-status"><span><i /> Connected to local workspace</span><span>Ln 1, Col 1&nbsp;&nbsp;&nbsp; UTF-8</span></div></section> }

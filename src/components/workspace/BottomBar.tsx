import React, { useState } from 'react'
import Glyph from './Glyph'
import '../../styles/BottomBar.css'
const tabs = ['Console', 'Git', 'Deploy', 'Logs', 'Errors', 'Terminal'] as const
export default function BottomBar() { const [active, setActive] = useState<(typeof tabs)[number]>('Console'); return <section className="bottom-bar"><nav className="bottom-tabs">{tabs.map(tab => <button key={tab} className={active === tab ? 'active' : ''} onClick={() => setActive(tab)}><span className={tab === 'Errors' ? 'tab-alert' : ''}>{tab === 'Terminal' ? '›_' : tab === 'Git' ? '⌘' : tab === 'Deploy' ? '↗' : '◌'}</span>{tab}{tab === 'Errors' && <b>0</b>}</button>)}<span className="bottom-spacer" /><button className="collapse-panel">⌃</button></nav><div className="bottom-content"><div className="console-prompt"><span>›</span><span className="muted-line">{active === 'Console' ? 'Console is ready. Run your project to see live output.' : `${active} activity will appear here.`}</span></div><div className="console-prompt"><span>›</span><i /></div></div><div className="bottom-status"><span><i /> Connected to local workspace</span><span>Ln 1, Col 1&nbsp;&nbsp;&nbsp; UTF-8</span></div></section> }
